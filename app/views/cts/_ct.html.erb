<tr>
  <td><%= ct.description %></td>
  <td><%= link_to 'Show', ct %></td>
	<% if current_user == ct.user %>
  		<td><%= link_to 'Edit', edit_ct_path(ct) %></td>
		<td><%= link_to 'Destroy', ct, method: :delete, data: { confirm: 'Are you sure?' } %></td>
	<% end %>	
</tr>